class Solution {
public:
    void moveZeroes(vector<int>& a) {
        int i,k=0;
        for(i=0;i<a.size();i++)
        {
            if(a[i]!=0)
            {
                a[k++]=a[i];
            }
        }
        for(;k<a.size();k++)
        {
            a[k]=0;
        }
    }
};