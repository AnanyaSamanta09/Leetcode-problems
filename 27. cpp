class Solution {
public:
    int removeElement(vector<int>& a, int val) {
     int i,k=0;
       for(i=0;i<a.size();i++)
        {
           if(a[i]!=val)
           {
               a[k]=a[i];
               k++;
           }
        } 
        return k;
    }
};